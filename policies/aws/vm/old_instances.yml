policies:
  - name: aws-vm-old-instances
    description: |
      Detect and terminate EC2 instances that are older than 90 days.
    resource: ec2
    filters:
      - type: value
        key: LaunchTime
        op: gt
        value: 90
        value_type: age
    actions:
      - terminate

